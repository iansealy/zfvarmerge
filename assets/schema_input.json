{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/iansealy/zfvarmerge/master/assets/schema_input.json",
    "title": "iansealy/zfvarmerge pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "gatk_vcf": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.vcf\\.gz$",
                "errorMessage": "GATK VCF file from zfvarcall pipeline must be provided, cannot contain spaces and must have extension '.vcf.gz'"
            },
            "freebayes_vcf": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.vcf\\.gz$",
                "errorMessage": "freebayes VCF file from zfvarcall pipeline must be provided, cannot contain spaces and must have extension '.vcf.gz'"
            },
            "bcftools_vcf": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.vcf\\.gz$",
                "errorMessage": "BCFtools VCF file from zfvarcall pipeline must be provided, cannot contain spaces and must have extension '.vcf.gz'"
            }
        },
        "required": ["sample", "gatk_vcf", "freebayes_vcf", "bcftools_vcf"]
    }
}
